{% extends 'base.html.twig' %}

{% block page_title 'Sitemap' %}

{% block content %}
    <ul>
        {% for page in pages %}
            <li><a href="{{ page.path }}">{{ page.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
